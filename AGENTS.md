# AGENTS.md

## Code style conventions

- Python code should use type annotations throughout the codebase.
- Annotate function parameters and return values, including `None` returns.
- Add variable annotations when the type is not obvious from the assigned value.
- Prefer explicit, readable types over clever shorthand when teaching concepts.
